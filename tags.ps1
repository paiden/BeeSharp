
# Version numbers (variants of these will be added to tags list)
$BEES_VERSION = "0.1.7"

# Other tags. Add the tag key and it s value. Tagger will search for
# .ttempl files and write that content with tag keys replaced with
# the respective values. In the ttempl file the key has to be written
# as $key$
$tags = @{}